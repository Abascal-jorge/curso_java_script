const producto = "Monitor curvo de 19 pulgadas gaming ";
const precio = "30 USD";

console.log(producto.concat(precio));
console.log(producto.concat("En descuento"));
console.log(`${producto} con precio de ${precio}`);
console.log( producto + precio);