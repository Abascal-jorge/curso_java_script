const producto = "Monitor curvo 20 pulgadas";

const texto = " En Promocion".repeat(3);

//.repetear te va a permitir repetir una cadena de tecto...
console.log(texto);
console.log(`${producto} ${texto} !!!`);


//Split, dividir un string 
const actividad = "Jorge juega todos los dias futbol";
console.log(actividad.split(" "));