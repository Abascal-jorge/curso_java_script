const  numeros = ["Enero", "Febrero", "Marzo", "Abril", "Mayo", "Junio", "Julio", "Agosto"];

console.table(numeros);

//Conocer el tamaño del arreglo
const tamano = numeros.length;

for ( let i=0; i<tamano; i++){
    console.log(numeros[i]);
}